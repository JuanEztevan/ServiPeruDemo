# Puerto que usa Render (importante para detectar el servicio)
server.port=${PORT:8080}

# Cadena de conexiÃ³n directa a Azure SQL (sin variables de entorno)
spring.datasource.url=jdbc:sqlserver://servi-serviperu.database.windows.net:1433;database=ServiPeru;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;
spring.datasource.username=Administrador@servi-serviperu
spring.datasource.password=J71126785j
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# ConfiguraciÃ³n adicional opcional para JPA si usas Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
